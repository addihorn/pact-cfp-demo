# Infrastructure Notes

- Runtime orchestration is defined in the repository `docker-compose.yml`.
- PostgreSQL is the data tier for local development.
- Migration SQL lives in `apps/api/migrations` and seed SQL in `apps/api/seed`.